## Overview

React Sizeme wraps React components to make them aware of their own dimensions, passing width and height as props to enable responsive behavior based on actual component size rather than viewport.

## Features

- Higher-order component for dimension awareness
- Width and height as props
- Automatic size tracking
- Resize event debouncing
- SSR-safe with placeholder sizes