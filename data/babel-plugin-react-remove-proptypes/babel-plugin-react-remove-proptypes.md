## Overview

A Babel plugin that removes unnecessary React PropTypes from production builds, reducing bundle size and improving application performance.

## Features

- Automatic PropTypes removal in production builds
- Configurable ignore list
- Works with Babel transform pipeline
- Reduces bundle size
- Zero runtime overhead