## Overview

React Portal transports children components to arbitrary DOM nodes (typically document.body), solving z-index stacking and positioning problems common with modals and overlays.

## Features

- Transport components to document.body
- Custom target DOM nodes
- Modal and lightbox support
- Loading bar and notification transport
- Clean mounting/unmounting lifecycle